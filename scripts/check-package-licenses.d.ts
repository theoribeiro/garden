#!/usr/bin/env ts-node
/**
 * Scans all package.json files in the repo and throws if one or more packages have a disallowed license
 * (i.e. GPL, other copyleft licenses).
 *
 * Stores a CSV dump
 */
export {};
//# sourceMappingURL=check-package-licenses.d.ts.map